export default {
  props : {
    // 标签颜色
    color : {
      type : String,
      default : ''
    },
    // 是否显示标签隐藏按钮
    closable : {
      type : Boolean,
      default : false
    },
    // 是否显示标签
    visible : {
      type : Boolean,
      default : false
    }
  },
  render () {
    const h = this.$createElement;
    
  }
}