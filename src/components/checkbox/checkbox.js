export default {
  props : {
    checked : Boolean,
    defaultChecked : Boolean,
    autoFocus : Boolean,
    disabled : Boolean
  },
  render () {
    const h = this.$createElement;
    return h(
      'span',
      {
        
      }
    )
  }
}