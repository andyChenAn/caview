export default {
  props : {
    // 数字
    count : {
      type : Number,
      default : 0
    },
    // 小圆点颜色
    color : {
      type : String,
      default : ''
    },
    // 偏移量
    offset : {
      type : Array,
      default : []
    }
  },
  render () {
    
  }
};